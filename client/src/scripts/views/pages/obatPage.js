import '../templates/obat_template';

const obatPage = {
    async render() {
        return `
            <obat-list></obat-list>
        `;
    }

    async afterRender() {
        const obat = await 
    }
}